# --- Web Framework ---
fastapi==0.116.1
uvicorn==0.35.0
python-multipart==0.0.20
python-dotenv==1.1.1
pydantic==2.11.7

# --- Google AI SDK ---
# main.py imports `google.generativeai` (the classic SDK), so we install that
# package — NOT `google-genai` (the new `google.genai` SDK with a different API).
google-generativeai

# --- Local ML Models (Required by your main.py imports) ---
transformers
torch