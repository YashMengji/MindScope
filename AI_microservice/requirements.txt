# --- Web Framework ---
fastapi==0.116.1
uvicorn==0.35.0
python-multipart==0.0.20
python-dotenv==1.1.1
pydantic==2.11.7

# --- New Google AI SDK ---
google-genai
# (Note: strictly excluded 'google-generativeai' to prevent conflicts)

# --- Local ML Models (Required by your main.py imports) ---
transformers
torch